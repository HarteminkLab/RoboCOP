Installation:

1. run setup.py
   python setup.py install --user

2. create shared object library
   cd mvcompete
   ./gccCompile

3. update shared object library path in mvcompete.py
   mvcompeteC = CDLL("<your new path>/libmvcompete.so")

Running:

1. To run using EM
   robocop.py <coordinate file -- e.g. unit_test/coord1.bed> <output directory>

2. To run without EM
   robocop_no_em.py <coordinate file -- e.g. unit_test/coord1.bed> <output directory generated by robocop.py> <output directory>

